package com.adp.eletter;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.modelmapper.ModelMapper;
import static org.junit.jupiter.api.Assertions.assertNotNull;

@SpringBootTest
public class EletterApplicationTest {

    @Autowired
    private ModelMapper modelMapper;

    @Test
    public void contextLoads() {
        // This method will simply load the context.
        // If there's any configuration error, the test will fail.
    }

    @Test
    public void testModelMapperBean() {
        assertNotNull(modelMapper, "ModelMapper bean should be initialized.");
    }
}
